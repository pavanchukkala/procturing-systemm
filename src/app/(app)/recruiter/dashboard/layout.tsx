// src/app/(app)/recruiter/dashboard/layout.tsx
export const dynamic = "force-dynamic";
export default function RecruiterDashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
